-- Trailing comma
class A {
  foo(x: Int): Int {
    let a : Int < 5,  hello:one <- clear, hello:one <- clear, hello:three <- clear, hello:four <- clear, hello:five <- clear, foo: Int < 5 in a;
  };
};
